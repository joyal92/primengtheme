// src/theme/primeng.preset.ts
import { definePreset } from '@primeuix/themes';

export default definePreset({
  components: {
    checkbox: {
      // width: '24px',           // 👈 Custom checkbox width
      // height: '24px',          // 👈 Ensure height matches if needed
      // iconSize: '16px',        // Optional: adjust checkmark size
      // borderRadius: '4px',     // Optional
      // borderColor: '#1976d2',  // Optional styling
      // backgroundColor: '#fff',
    }
  }
});
